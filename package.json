{
  "name": "ash-county",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "test": "tsc src/sim.ts src/inventory.ts --target ES2022 --module ES2022 --skipLibCheck --outDir .test-build && node --test tests/*.test.mjs",
    "test:render": "node tests/render3d.mjs",
    "assets:models": "node scripts/export-models.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/three": "^0.185.4",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
